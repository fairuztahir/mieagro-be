apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.22.0 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: proxy
  name: proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: proxy
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.22.0 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.service: proxy
    spec:
      containers:
        - image: fairuztahir/qp-proxy
          name: proxy
          ports:
            - containerPort: 80
            - containerPort: 443
          resources: {}
          volumeMounts:
            - mountPath: /app
              name: proxy-data
      imagePullSecrets:
      - name: regcred
      restartPolicy: Always
      volumes:
        - name: proxy-data
          persistentVolumeClaim:
            claimName: proxy-data
status: {}
